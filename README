0. Build base image by docker-compose buil base
1. Run startMe.sh at first terminal tab to init containers. In stdout of GW you will see tcpdump for 80 port
2. At other terminal runs docker exec nginx_client curl -X PUT http://gw/body -d@/tmp/body
3. See requests and redirects at first terminal 
